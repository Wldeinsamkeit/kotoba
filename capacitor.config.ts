import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tiantian.nihongodaily',
  appName: 'Nihongo Daily',
  webDir: 'dist'
};

export default config;
